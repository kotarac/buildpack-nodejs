#!/usr/bin/env zsh
set -e

BIN=$1

if [ -z "$1" ]; then
  echo "no tool specified to use" && false
fi

shift

if [ "$BIN" = "npm" ]; then
  .heroku/node/bin/npm "$@"
else
  .heroku/$BIN/bin/$BIN "$@"
fi
