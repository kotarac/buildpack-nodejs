#!/usr/bin/env bash
set -e

BIN=$1
BUILD_DIR=`pwd`/.buildpack-nodejs
export PATH=$BUILD_DIR/node/bin:$PATH

if [ -z "$BIN" ]; then
  echo "no tool specified to use" 1>&2
  exit 1
fi

shift

$BIN "$@"
